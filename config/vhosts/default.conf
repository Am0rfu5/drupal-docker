<VirtualHost *:80>
    ServerName ${VIRTUALHOST_SERVER_NAME}
    DocumentRoot ${APACHE_DOCUMENT_ROOT}
    <Directory ${APACHE_DOCUMENT_ROOT}>
        AllowOverride all
        allow from all
        Options +Indexes
    </Directory>
    Loglevel warn
</VirtualHost>
